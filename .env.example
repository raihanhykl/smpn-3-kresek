# Data source toggle. Defaults to "static" (config files).
# Set to "api" when wiring up a future backend implementation.
NEXT_PUBLIC_DATA_SOURCE=static

# Base URL for the future ApiContentProvider (e.g. https://api.example.com/v1).
# Unused while NEXT_PUBLIC_DATA_SOURCE=static.
NEXT_PUBLIC_API_BASE_URL=
